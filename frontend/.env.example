# URL of the deployed backend API. Leave blank to run fully offline using the
# bundled demo songs & albums (great for static demos / interviews).
# Example: https://your-backend.onrender.com
VITE_API_URL=

# Password required to open the /admin panel (defaults to "Redn1nja" if unset).
VITE_ADMIN_PASSWORD=
